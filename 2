#include <iostream>
using namespace std;
int b = 0;
int c = 0;
int rec(int a) {
	if (a == 0) {
		return c;
	}
	c += ((a % 10) * (pow(2, b++)));
	rec(a / 10);
}
int main() {
	int a;
	cin >> a;
	cout << rec(a);
}
